{
  "name": "micropig-website",
  "type": "module",
  "version": "0.0.1",
"scripts": {
  "dev": "astro dev",
  "start": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "preview:github": "astro preview --host",
  "astro": "astro",
  "deploy": "npm run build && touch dist/.nojekyll && npx gh-pages -d dist"
},
  "dependencies": {
    "@astrojs/tailwind": "^5.0.0",
    "astro": "^3.0.12",
    "tailwindcss": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.5.7",
    "gh-pages": "^6.3.0",
    "typescript": "^5.2.2"
  }
}
