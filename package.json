{
  "name": "micropig-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "node build-setup.js && astro build",
    "preview": "astro preview",
    "preview:github": "astro preview --host",
    "astro": "astro",
    "deploy": "npm run build && touch dist/.nojekyll && npx gh-pages -d dist",
    "setup": "node build-setup.js"
  },
  "dependencies": {
    "@astrojs/tailwind": "^5.0.0",
    "astro": "^3.0.12",
    "jsdom": "^22.1.0",
    "node-fetch": "^2.7.0",
    "tailwindcss": "^3.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.5.7",
    "gh-pages": "^6.3.0",
    "typescript": "^5.2.2"
  }
}